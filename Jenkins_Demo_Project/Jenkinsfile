pipeline {
    agent any
    stages {
        stage('Build & Test') {
            steps {
                // Phải có dòng này vì code của ông nằm trong thư mục con
                dir('Jenkins_Demo_Project') {
                    bat 'mvn clean test'
                }
            }
        }
    }
}
